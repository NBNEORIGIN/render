# Session — initial build of Render from SignMaker (2026-03-30)
Tags: session history signmaker migration decisions
Date: 2026-03-30

## Context

Cairn reviewed the original NBNEORIGIN/signmaker repo and advised on improvements.
A new repo NBNEORIGIN/render was created at D:\render.
This session implemented the full architectural overhaul and deployed to the live server.

## Work completed in this session

### Architecture improvements
- Removed Cloudflare R2 — own-server image hosting via nginx
- Database-driven blanks system replacing hardcoded SIZES dicts
- Extracted HTML from monolithic app.py to templates/index.html
- Path traversal fix in download_m_folders

### New files created
- `local_storage.py` — replaces r2_storage.py
- `config.py` — full rewrite, added BLANK_SEEDS, DEFAULT_USERS, SMTP config
- `models.py` — added User model, Blank model, _seed_users, _seed_blanks
- `templates/index.html` — extracted frontend (2,794 lines)
- `docker-compose.yml` — PostgreSQL sidecar + app container
- `docker/nginx/render.nbnesigns.co.uk.conf` — nginx reverse proxy + SSL
- `deploy.sh` — first-time server setup script
- `PROTOCOL.md` — project protocol document
- `memory/` — hybrid BM25 + cosine semantic memory system

### Authentication
- Replaced flask-httpauth single Bearer token with Flask session email/password
- Five users seeded: Gabby, Toby, Sanna, Ivan, Ben
- Fixed CSS brace KeyError in LOGIN_HTML (used .replace() instead of .format())

### UI
- Renamed "SignMaker" → "Render" throughout
- Bug report modal with IONOS SMTP email delivery

### Deployment
- Server: 178.104.1.152 (in-house Ubuntu, not Hetzner)
- Docker project name `-p render` must be used to keep container naming consistent
- .env lives at /opt/nbne/render/.env (one level above the git repo in /opt/nbne/render/app/)
- nginx 1.24 quirk: `listen 443 ssl http2;` not `http2 on;` directive

## Key decisions

| Decision | Chosen |
|---|---|
| Image hosting | Own nginx, no R2 |
| Auth | Email/password sessions |
| Blanks | DB-driven |
| Workers | 1 (job state in-memory) |
| Deployment target | Own Ubuntu server, not Render.com or Hetzner |
| HTML | Extracted to templates/ |
